pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp index.html game.js styles.css public/
  pages: true
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
