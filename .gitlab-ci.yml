image: alpine:latest

create-pages:
  pages:
    publish: public
  before_script:
    - ":"
  script:
    - mkdir -p public
    - cp index.html game.js styles.css public/
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
