pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html game.js styles.css public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
